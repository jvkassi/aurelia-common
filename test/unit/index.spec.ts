// import { configure } from '../../src/aurelia-erp-commonss';

describe('configure()', () => {

  it(`dummy test`, () => {
    const test = true;
    expect(test).toBe(true);
    // const fxconfig: any = {};

    // configure(fxconfig);

    // expect(fxconfig.foo).toBe('bar');
  });
});
